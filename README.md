# rh-lp-tools

Public download page for the fee-charging builds of RH Pool, RH Portfolio and
the RH Secrets template. Static: `index.html` plus `scripts/`.

The scripts are generated, not edited here. Rebuild them from the apex-hood
repository with `node tools/build-scripts.mjs`, then copy `build/public/*.js`
into `scripts/`.
